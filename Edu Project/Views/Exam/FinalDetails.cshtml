@model Edu_Project.Models.FinalExam

@{
    ViewData["Title"] = "Final Exam Details";
}

<section class="edu-page-section">

    <div class="container">

        <div class="edu-page-header">

            <span class="edu-hero-badge">
                Final Exam
            </span>

            <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3">

                <div>

                    <h1 class="edu-page-title">
                        @Model.Title
                    </h1>

                    <p class="edu-page-subtitle mb-0">
                        Review the final exam information, questions and answers.
                    </p>

                </div>

                <div class="d-flex gap-2 flex-wrap">

                    <a asp-action="EditFinal"
                       asp-controller="Exam"
                       asp-route-id="@Model.Id"
                       class="btn edu-btn-primary">

                        Edit Final Exam

                    </a>

                    <a asp-controller="Course"
                       asp-action="Details"
                       asp-route-id="@Model.courseId"
                       class="btn edu-btn-outline">

                        Back to Course

                    </a>

                </div>

            </div>

        </div>

        <div class="quiz-details-summary">

            <div class="quiz-summary-main">

                <div class="quiz-summary-icon">
                    F
                </div>

                <div>

                    <span class="dashboard-panel-label">
                        Final Exam Overview
                    </span>

                    <h2>
                        @Model.Title
                    </h2>

                    <p>
                        This exam contains
                        @(Model.Question?.Count ?? 0)
                        question@(Model.Question?.Count == 1 ? "" : "s").
                    </p>

                </div>

            </div>

            <div class="quiz-summary-stats">

                <div class="quiz-summary-stat">

                    <span>
                        Duration
                    </span>

                    <strong>
                        @Model.Duration min
                    </strong>

                </div>

                <div class="quiz-summary-stat">

                    <span>
                        Total Marks
                    </span>

                    <strong>
                        @Model.TotalMarks
                    </strong>

                </div>

                <div class="quiz-summary-stat">

                    <span>
                        Questions
                    </span>

                    <strong>
                        @(Model.Question?.Count ?? 0)
                    </strong>

                </div>

            </div>

        </div>

        <div class="quiz-questions-header">

            <div>

                <span class="dashboard-panel-label">
                    Question List
                </span>

                <h2>
                    Final Exam Questions
                </h2>

                <p>
                    Correct answers are highlighted below.
                </p>

            </div>

        </div>

        @if (Model.Question != null &&
             Model.Question.Any())
        {
            var questionNumber = 1;

            <div class="quiz-question-list">

                @foreach (var question in Model.Question)
                {
                    <div class="quiz-question-card">

                        <div class="quiz-question-number">
                            @questionNumber
                        </div>

                        <div class="quiz-question-content">

                            <span class="quiz-question-label">
                                Question @questionNumber
                            </span>

                            <h3>
                                @question.Text
                            </h3>

                            @if (question.Answers != null &&
                                 question.Answers.Any())
                            {
                                <div class="quiz-answer-list">

                                    @{
                                        var answerNumber = 1;
                                    }

                                    @foreach (var answer in question.Answers)
                                    {
                                        <div class="quiz-answer-item @(answer.IsCorrect ? "correct" : "")">

                                            <div class="quiz-answer-letter">

                                                @if (answerNumber == 1)
                                                {
                                                    <span>A</span>
                                                }
                                                else if (answerNumber == 2)
                                                {
                                                    <span>B</span>
                                                }
                                                else if (answerNumber == 3)
                                                {
                                                    <span>C</span>
                                                }
                                                else
                                                {
                                                    <span>D</span>
                                                }

                                            </div>

                                            <span class="quiz-answer-text">
                                                @answer.Text
                                            </span>

                                            @if (answer.IsCorrect)
                                            {
                                                <span class="quiz-correct-badge">
                                                    Correct Answer
                                                </span>
                                            }

                                        </div>

                                        answerNumber++;
                                    }

                                </div>
                            }
                            else
                            {
                                <div class="quiz-no-answers">
                                    No answers found for this question.
                                </div>
                            }

                        </div>

                    </div>

                    questionNumber++;
                }

            </div>
        }
        else
        {
            <div class="dashboard-panel">

                <div class="dashboard-empty-state">

                    <div class="dashboard-empty-icon">
                        F
                    </div>

                    <h4>
                        No questions found
                    </h4>

                    <p>
                        This final exam does not contain any questions yet.
                    </p>

                </div>

            </div>
        }

        <div class="mt-4">

            <a asp-controller="Course"
               asp-action="Details"
               asp-route-id="@Model.courseId"
               class="btn edu-btn-outline">

                ← Back to Course

            </a>

        </div>

    </div>

</section>